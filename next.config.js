/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["logo.moralis.io"], // Add any additional domains if needed
  },
};

module.exports = nextConfig;
